<%= form_for(@presupuesto) do |f| %>
  <% if @presupuesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@presupuesto.errors.count, "error") %> prohibited this presupuesto from being saved:</h2>

      <ul>
      <% @presupuesto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col-sm-6">
    <div class="form-group">
      <%= f.label :codigoalg %><br>
      ALG-2018-
      <%= f.text_field :codigoalg, class:"form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :cliente_id %><br>
      <%= f.select("cliente_id", Cliente.all.order('nombre ASC').collect {|p| [p.nombre, p.id]}, { include_blank: true }) %>
      No existe el cliente ? <%= link_to 'Nuevo Cliente' , new_cliente_path, class:"btn btn-sm btn-primary" %>
    </div>
    <div class="form-group">
      <%= f.label :referencia %><br>
      <%= f.text_field :referencia, class:"form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :series %><br>
      <%= f.text_field :series, class:"form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :acabado %><br>
      <%= f.text_field :acabado, class:"form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :estado %><br>
      <!--<%= f.text_field :estado, class:"form-control" %> -->
      <%= check_box_tag(:estado, 'PENDIENTE', true) %> PENDIENTE
      <%= check_box_tag(:estado, 'OK') %> OK
      <%= check_box_tag(:estado, 'ENVIADO') %> ENVIADO
      <%= check_box_tag(:estado, 'PEDIDO') %> PEDIDO

    </div>
    <div id="name" class="form-group">
        <%= f.label :etiquetas %><br />
        <%= select_tag(:etiquetas, options_for_select([["A limpio", 1],["Recálculo", 2]])) %>
    </div>

    <div class="form-group">
      <%= f.submit class:"btn btn-primary" %>
    </div>

  </div>
</div>

<% end %>
